# amogus
Программа для автоматической предзаписи на курсы в Модеус.
С её помощью можно выбрать нужные предметы, сохранить выбор в файл, а потом поставить таймер, который сам сделает запись в нужное время.

## Запуск
1. Клонируй репозиторий
`git clone https://github.com/Matvke/amogus.git`
2. Перейди в рабочую директорию
`cd amogus`
3. Создай виртуальное окружение
`python -m venv .venv`
4. Активируй виртуальное окружение
Windows
`.\.venv\Scripts\activate`
Linux/MacOS
`source .venv/bin/activate`
5. Установи проект
    1. Для разработки
        `pip install -e .`
    2. Для обычного использования
        `pip install .`
6. Получи код меню из URL меню выбора и впиши его в .env
`https://urfu.modeus.org/learning-path-selection/api/selection/menus/{menu_id}`
Нужно именно `menu_id`, а не вся ссылка.
> Пример .env фала можешь посмотреть в .env.example
7. Получи JWT токен и запиши его в .env
    1. Для этого перейди на страницу выбора
    2. Перейди в панель админа `f12`
    3. Вкладка Storage -> Session Storage
    4. Скопируй значение `id_token` в .env в поле token
8. Запусти TUI приложение
`amogus` 
> Можно явно указывать .env file `amogus --env-file ".env1"`   

## Структура проекта

```
.
├── LICENSE                    
├── pyproject.toml             
├── README.md                  
└── src                        
    ├── app.py                 # Основной класс приложения Textual (AmogusApp)
    ├── color.tcss             # Стилизация для TUI (Textual CSS)
    ├── exceptions             # Пользовательские исключения
    │   └── api_exc.py         # Исключения для работы с API
    ├── __init__.py            
    ├── main.py                # Точка входа (CLI), запуск приложения
    ├── models                 # Модели и структуры данных
    │   ├── entities.py        # Сущности приложения (PushResult, выбор дисциплин и т.п.)
    │   └── settings.py        # Конфигурация приложения, переменные окружения
    ├── services               # Сервисы для бизнес-логики и взаимодействия с API
    │   ├── api_client.py      # Синхронный API Client (requests)
    │   ├── async_client.py    # Асинхронный API Client (httpx.AsyncClient)
    │   ├── cycle_service.py   # Работа с курсами дисциплин
    │   ├── modulegroup_service.py # Работа с группами дисциплин
    │   ├── push_service.py    # Сервис записи на выбранные дисциплины (Push)
    │   ├── select_service.py  # Управление выбором дисциплин пользователем
    │   ├── storage_service.py # Локальное хранилище 
    │   └── timer_service.py  # Таймер для отсчёта и автоматического push
    └── views                  # Виджеты и компоненты UI
        └── menu_tree.py       # Визуальное отображение структуры меню/дисциплин
```

## Переменные .env
```
menu_id=<Твой UUID4>
token=<Твой JWT токен>
pick_time=<дата и время выбора предметов>
```
> Формат даты: %Y-%m-%d %H:%M например: "2026-03-31 16:33"

## Использование
> Символ `^` в интерфейсе приложения означает клавишу `Ctrl`.

Выбираешь команды сочетанием `ctrl+s`, сохраняешь их в файл (по желанию), запускаешь таймер для записи через `ctrl + v`

### Горячие клавиши

| Клавиша | Действие |
| :--- | :--- |
| `Arrows` (Стрелки) | Навигация по интерфейсу |
| `Tab` | Переключение со вкладок на рабочую область |
| `Ctrl + x` | Сохранить выбранные дисциплины в файл |
| `Ctrl + z` | Загрузить список команд из файла |
| `Ctrl + v` | Отправить запрос на запись в выбранные команды |
| `Ctrl + t` | Поставить таймер на запись в выбранные команды |
| `Ctrl + q` | Выйти из приложения |


### Работа с деревом (Вкладка Меню выбора)

В этой вкладке вы управляете выбором конкретных команд:

*   **`Enter`/`Space`** — Развернуть или свернуть ветку (ноду).
*   **`Ctrl + s`** — Выбрать/Убрать текущую команду.  

### Работа с таблицей результатов (Вкладка Результаты записи)

В этой вкладке вы можете посмотреть результат записи:

| Lesson ID | Status | Message |
| :--- | :--- | :--- |
| ID предмета | Статус (Успех/ошибка) | Сообщение об ошибках |


### Contribute 
Если хочешь помочь amogus, можно:

- Исправлять ошибки или улучшать стабильность приложения.
- Добавлять новые фичи или улучшать интерфейс TUI.
- Искать TODO в коде и выполнять задачи
- Писать тесты для существующих сервисов.
- Помогать с документацией или примерами использования.

Все изменения присылайте через Pull Request.